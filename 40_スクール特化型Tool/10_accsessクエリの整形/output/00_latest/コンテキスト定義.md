# コンテキスト定義（初版）

このファイルは、ER推定・差分抽出・テーブル選別の精度を上げるためのプロジェクト前提ルールです。  
`確定` と `推定` を分けて記載します。

## 1. フラグ運用（Flag Semantics）

### 1-1. 対象テーブル
- table: `T利用者`

### 1-2. 主要フラグ（現時点）
- `受講生`
- `講師`
- `一般研修`

### 1-3. ルール
- status: `推定`
- rule: 1レコードにつき、上記フラグのいずれかが業務区分を表す（排他の可能性が高い）
- note: SQL内で `受講生/講師/一般研修` を `Switch(...)` で利用区分に変換している記述あり

### 1-4. 判定優先順位（暫定）
- status: `未定`
- priority_if_conflict: 未設定（運用担当確認待ち）

## 2. テーブル命名規則（運用傾向）

### 2-1. 中間・作業・バックアップ系の傾向
- status: `推定`
- pattern_examples:
  - prefix: `TEMP_`
  - prefix: `FIX_`（変換由来ノイズの可能性あり）
  - contains: `一時`
  - contains: `作業`
  - contains: `変更用`
  - contains: `削除`
  - contains: `復元`
  - prefix: `Q`（クエリ相当オブジェクト）

### 2-2. ER作成時の基本方針
- status: `確定（本分析運用）`
- policy:
  - `core` 図では上記パターンを原則除外
  - `full` 図では保持し、用途別（受講生/講師/一般研修）で差分化

## 3. コアテーブル候補（現時点）

- status: `推定`
- likely_core_tables:
  - `T利用者`
  - `T講座申込書`
  - `T講座実施DB`
  - `T各回講座開催情報`
  - `TM会社`
  - `TM勤務地`
  - `TM受講制限区分`
  - `TM受講可否`
  - `TM必修対象者`
  - `TM料金`
  - `TM宿泊室`
  - `TMSF共通ID`

## 4. フラグ別に注目するテーブル（初期案）

### 4-1. 受講生
- status: `推定`
- include_priority:
  - `TM受講制限区分`
  - `TM受講可否`
  - `TM必修対象者`
  - `TM料金`
  - `TM会社`
  - `T講座実施DB`

### 4-2. 講師
- status: `推定`
- include_priority:
  - `T講座実施DB`
  - `TM宿泊室`
  - `TM会社`
  - `TM勤務地`
  - `TM料金`

### 4-3. 一般研修
- status: `推定`
- include_priority:
  - `T講座実施DB`
  - `TM料金`
  - `TM宿泊室`
  - `TM受講可否`
  - `TM会社`

## 5. 関係推定ルール（Project Specific）

### 5-1. 優先するJOIN根拠
- status: `確定（本分析運用）`
- rule:
  - `A.col = B.col` を最優先
  - `FROM/JOIN/WHERE` の等価条件を採用

### 5-2. FK候補パターン
- status: `確定（本分析運用）`
- fk_patterns:
  - `*_id`
  - `ID_*`
  - `*コード`
  - `*番号`

### 5-3. 注意が必要な結合
- status: `確定（本分析運用）`
- caution:
  - `氏名` など自然キー結合は同姓同名リスクがあるため `暫定`
  - `FIX_` 列は変換由来の可能性があるため、実定義確認までは `暫定`

## 6. 既知の高信頼リンク（初版）

- status: `推定（高）`
- high_confidence_relations:
  - `T利用者.会社コード = TM会社.会社コード`
  - `T利用者.受講可否 = TM受講可否.受講可否コード`
  - `T利用者.受講制限評価用 = TM受講制限区分.受講制限評価用`
  - `T利用者.講座秘匿性レベル = TM受講制限区分.講座秘匿性レベル`
  - `T利用者.分割開催回 = T講座実施DB.分割開催回`
  - `T利用者.開催講座コード = T講座実施DB.開催講座コード`

## 7. 今後の更新ルール（運用）

- このファイルに追記する際は、各項目に `status: 確定/推定/未定` を付ける
- 仕様確定後は `推定` を `確定` に更新し、根拠SQLまたは仕様書リンクを添える
- 新しいフラグ区分（10フラグ運用）が判明したら、`1` と `4` を最優先で更新する

