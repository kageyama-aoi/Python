# 実装計画: UI安定性と一貫性の改善

## 目的

- 画面遷移時の「横幅ズレ」「ちらつき感」を減らし、利用時ストレスを下げる。
- `/`（メタ編集）・`/import`（取込）・`/kb`（一覧）の見た目と操作感を統一する。

## 参照ドキュメント

- `docs/UI安定性と一貫性デザイン改善ガイド.md`

## 対象範囲

- `app.py` のHTML/CSS（`/` と `/import`）
- `build.py` の `index.html` 生成テンプレート（`/kb` 相当）
- 必要に応じて `html/style.css` または新規 `html/shared_ui.css`
- `README.md`（運用変更がある場合のみ）

## スコープ外

- 情報設計そのものの大幅変更（項目追加・機能追加）
- バックエンドAPI仕様の変更

## 成果物

1. 共通トークン（幅・余白・タイポ）の定義
2. 3画面共通のヘッダー/ナビスタイル
3. フォーム部品の寸法統一
4. メッセージ表示領域の高さ固定（レイアウトジャンプ抑制）
5. 確認結果メモ（本ファイル末尾に追記）

## 実装ステップ

## Step 1: 現状差分の固定

- 3画面の現状値を洗い出し
  - コンテナ幅
  - 余白
  - フォントサイズ
  - ブレークポイント
- 基準値を決定
  - `--container-max`（候補: 1120px）
  - `--space-*`
  - `--radius-*`

## Step 2: 共通スタイル導入

- 共通CSSを定義（既存 `html/style.css` 拡張 or `html/shared_ui.css` 新設）
- 以下を共通クラス化
  - ページコンテナ
  - ヘッダー/ナビ
  - ツールバー
  - フォームコントロール
  - ステータスメッセージ領域

## Step 3: メタ編集画面（`/`）適用

- `app.py` 内のCSSを共通化方針に沿って更新
- 既存UI機能（検索・一括編集・保存）を壊さないことを確認

## Step 4: 取込画面（`/import`）適用

- `/` と同じ骨格にそろえる
- カテゴリ/タグ候補UIの見た目密度を統一
- 保存結果表示で上下ジャンプが発生しないよう固定領域化

## Step 5: 一覧画面（`/kb` / `build.py`生成）適用

- `build.py` テンプレートの寸法・余白を共通化
- 生成後の `html/index.html` で見た目が一致することを確認

## Step 6: 検証

- 画面幅: `360 / 768 / 1280`
- ズーム: `200% / 400%`
- 操作: 保存前後・検索時・タグ操作時のレイアウト安定
- CLI確認:
  - `python -m py_compile app.py`
  - `python build.py`

## 受け入れ条件

- 3画面でコンテナ幅・余白・部品寸法が実質統一されている
- 保存/更新操作で主要UIの位置が大きく跳ねない
- 既存機能（メタ編集・取込保存・ビルド生成）が回帰していない
- モバイル幅でも崩れず操作可能

## リスクと対策

- リスク: `app.py` と `build.py` が別経路でHTMLを出しているため再分岐しやすい  
  対策: トークン名と値を同一化し、差分レビュー項目に追加

- リスク: インラインCSSの残存で部分的に古い見た目が残る  
  対策: 実装完了時に「共通化漏れチェック」を実施

## 作業チェックリスト

- [x] 基準トークンを確定した
- [x] `/` を共通化した
- [x] `/import` を共通化した
- [x] `/kb`（`build.py`）を共通化した
- [ ] 3幅 + 2ズームで確認した
- [ ] READMEの必要箇所を更新した

## 追加課題（2026-02-18）

- 現象: `/import` 画面でちらつき感が残る
- 仮説: 「グレー背景」+「白コンテナ」+「白フォーム（特に大きいtextarea）」のレイヤー重なりで視線が落ち着かない
- 追加対応方針:
  - コンテナとフォーム背景の階層を1段減らす（白レイヤーの重なりを削減）
  - textareaの背景を薄色へ変更し、主従を明確化
  - 枠線コントラストを弱めて境界のチラつき感を抑える
  - 変更後に 360/768/1280 + 200%/400% で再確認

### 追加課題チェック

- [x] `/import` の白レイヤー重なりを削減するスタイル変更を実装した
- [x] textarea背景を薄色に変更した
- [x] 入力部品/境界のコントラストを調整した
- [ ] 画面幅とズーム条件で実機確認した

## 追加要望（2026-02-18）

- メタ編集画面の縦方向密度を上げて、一覧性を改善する
- メタ編集画面でMDファイル名を編集（リネーム）できるようにする

### 追加要望チェック

- [x] タグ編集UIを折りたたみ化してカード高さを圧縮した
- [x] ファイル名入力欄を追加した
- [x] `/api/update` にリネーム処理を追加した
- [ ] 実機で一覧性向上を目視確認した

## 実装ログ（追記用）

- 着手: 2026-02-18
- 完了: （未）
- 備考:
  - 2026-02-18: 3画面の幅トークンを `1120px` に統一（`/`, `/import`, `/kb`）
  - 2026-02-18: ナビ/入力部品の寸法と最小高さを統一
  - 2026-02-18: `/import` の結果表示領域に `min-height` を設定し、保存時ジャンプを抑制
  - 2026-02-18: 追加課題として「白レイヤー重なりによる視覚ちらつき」を記録
  - 2026-02-18: `/import` の背景レイヤーを削減（白コンテナ廃止、フォーム薄色化、textarea薄色化、境界コントラスト調整）
  - 2026-02-18: Flask test client で `/, /import, /kb` のCSSトークン整合を確認（`--max: 1120px`、`max-width: 960px` 非存在、`--max: 1200px` 非存在）
  - 2026-02-18: 3幅 + 2ズームの実機目視確認は未実施（次アクション）
