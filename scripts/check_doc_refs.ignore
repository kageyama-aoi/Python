# check_doc_refs.py が「実在しないが意図的な参照」を警告しないための除外リスト。
# 1行1件。# 以降はコメント。glob 可（例: 90_ひな形/templates/*.py）。
# パスは / 区切り。末尾のファイル名だけでも可（例: some_generated_file.py）。
#
# ここに足すときは「なぜ実在しないのが正しいのか」をコメントで残すこと。

# 別リポジトリ（Tools\testcode\e2e）の設計を踏襲した旨の言及。当リポジトリには無くて正しい。
e2e/run/run_gui.py

# 30_ の「経緯」節が説明する、統合前の旧スクリプト配置と退避先。歴史的記述で実在しないのが正しい。
_archive/72_73_旧データ

# 40_ の commit_suggest.ps1 がプロンプトを探す候補パスの1つ。存在すれば使う、が既定では無い。
scripts/commit_prompt.md

# 60_Data_Flow_Portal(データフロー書き出し)/AGENTS.md の「モジュール命名例:」に挙がる例示。
# 実体は P01/src/handlers/ 側にある。命名の例であってパスの主張ではない。
handlers/csv_loader.py
utils/log.py
