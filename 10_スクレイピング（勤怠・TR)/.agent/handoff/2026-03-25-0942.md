# HANDOFF - 2026-03-25 (最新)

## 使用ツール
Claude Code (claude-sonnet-4-6)

## 現在のタスクと進捗
- [x] Seleniumステータス保存バグ修正：`select[name='status_edit']` CSSセレクタ修正 + `change`イベント dispatch
- [x] リファクタリング Phase 1〜5：main.py 削減、shimamura_search_handler.py 抽出、handler_factory.py リネーム、gui_config_dialog.py 分離、config ファイル整理
- [x] ナレッジ集作成：docs/knowledge_beginner.md / intermediate.md / advanced.md
- [x] プロジェクト初期セットアップ (make_project Mode B)：AGENTS.md, .spec/, .agent/, .claude/commands/, .github/ISSUE_TEMPLATE/ 整備
- [x] GUI レイアウト改善 (Issue #32)：マージ依頼 (Shimamura) を TR詳細設定セクション内に移動、キーワード入力を隣接配置、`_on_submit()` の検証ロジック更新

## 試したこと・結果
- `select_by_visible_text()` だけでは JS の change イベントが発火せず保存されなかった → `execute_script` で `dispatchEvent` を追加して解決
- `select[name='status']` を対象にしていたが実際のページには `select[name='status_edit']` が正しいセレクタ → 修正済み
- GUI で「マージ依頼」が Section 1 (モード選択) にあり、キーワード入力が Section 3 にあって離れすぎていた → Section 2 (TR詳細設定) 内にセパレータ区切りで統合 (Option A)

## 次のセッションで最初にやること
1. GUI の動作確認 (実際に起動して TR モード → マージ依頼選択 → キーワード入力 → 実行 が正常に動くか確認)
2. .spec/PLAN.md, SPEC.md, TODO.md に現状を記載していないため、仕様書の整備を検討
3. 必要に応じて次の機能追加や改善 Issue を起票

## 注意点・ブロッカー
- `gui.py` の `_on_submit()` で `mode == 'tr'` かつ `final_school_type == 'search'` のパスが新しいロジック。旧コードの `elif mode == 'search':` ブランチは削除済みなので混同しないこと
- `handler_factory.py` は旧名 `form_handler.py` から rename 済み。`main.py` の import も更新済み (`from handler_factory import HandlerFactory`)
- `.pycache` が git 管理下に入り込んでいる (`.gitignore` 未設定の可能性あり)。コミット時に不要な `.pyc` が含まれないよう注意
