# HANDOFF - 2026-03-25（午後）

## 使用ツール
Claude Code (claude-sonnet-4-6)

## 現在のタスクと進捗
- [x] GUI レイアウト改善 (Issue #32)：`_on_submit()` 更新・コミット完了
- [x] マージ依頼フローに保存ボタンクリック追加 (Issue #33)：`input[name='send']` クリック実装
- [x] 処理結果サマリー一覧表を最後に必ず表示 (Issue #34)：`_print_summary()` 新設、ターミナル出力
- [x] 処理結果サマリーをテキストファイルに出力 (Issue #35)：`data/summary_{日付}.txt` に書き出し
- [ ] Title 列インデックスの特定と修正：デバッグ用CSV出力コードを追加済み（未検証）

## 試したこと・結果
- `tds[1]` を Title として取得したが、実ページの Title（例：「確認中_0325」）とは異なる値が取れていた
- 全列デバッグ用CSV（`data/debug_columns_{日付}.csv`）を出力するコードを追加。次回実行で正しい列インデックスが判明する

## 次のセッションで最初にやること
1. プログラムを実行して `data/debug_columns_{日付}.csv` を確認し、`確認中_0325` が入っている列番号（`tds[n]`）を特定する
2. `_extract_results()` の `title = tds[1]` を正しいインデックスに修正する
3. デバッグ用CSV出力コードを削除してクリーンアップし、コミット
4. 途中で届いた `<input name="what_edit" value="本番環境_更新依頼">` の意図を確認する（別タスクの可能性あり）

## 注意点・ブロッカー
- `debug_columns` CSV は `utf-8-sig` で出力しているのでExcelで直接開ける
- デバッグコードはあくまで一時的なもの。Title列特定後は必ず削除すること
- サマリーTXTと同日に複数回実行すると `data/summary_{日付}.txt` が上書きされる（現状は追記ではなく上書き）
