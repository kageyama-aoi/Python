# HANDOFF - 2026-03-26

## 使用ツール
Claude Code (claude-sonnet-4-6)

## 現在のタスクと進捗
- [x] `docs/07_Python構文集.md` に「❌ 使わなかった場合」比較コードを追加（中級9〜16・上級17〜21、全13構文）
- [ ] プロジェクトルート直下の整理（Phase 1〜3）：プランのみ提示、未着手

## 試したこと・結果

### 07_Python構文集.md の追記（成功）
- 中級・上級の各構文（計13件）に `#### ❌ 使わなかった場合` セクションを追加
- コード例と「発生する問題」を箇条書きで記載
- 628行 → 917行 に増量
- 追加した各構文の主なポイント：
  - リスト内包表記：for+append の冗長さ・初期化忘れ
  - next()+ジェネレータ：break忘れバグ・デフォルト値見落とし
  - 三項演算子：else節書き忘れによる NameError
  - lambda：使い捨て関数への不要な命名
  - クロージャ：グローバル変数汚染・複数ダイアログ干渉
  - isinstance()：str に .items() を呼ぶ AttributeError
  - global宣言：再代入がローカル変数扱いになるサイレントバグ
  - 再帰：階層上限問題・保守コスト増大
  - 型ヒント：引数ミスが実行時まで発覚しない
  - 抽象クラス：タイポで実装漏れがサイレントスキップ
  - デコレータ：fixture呼び出し書き忘れ・テストコード冗長化
  - sys.path：ImportError・重複コードの散在
  - __file__：ハードコードパスの環境依存バグ

### プロジェクト整理プランの策定（提示のみ）
現状の散らかりを調査し、3フェーズのプランを提示した：
- **Phase 1**（コード変更なし）：ログ7本を `logs/` へ、デバッグCSVを削除、`test_handlers.py` / `dry_run_check.py` の用途確認・整理
- **Phase 2**（コード変更あり）：`data/screenshots/`・`data/summary/`・`logs/app/` を新設し、出力先パスをコードで変更（main.py:16、shimamura_search_handler.py:102,158）
- **Phase 3**（将来対応）：出力先パスを `config/main.yaml` で管理できるよう設定ファイル化

## 次のセッションで最初にやること

1. **Phase 1 の着手確認**：`dry_run_check.py` と `test_handlers.py` の用途をユーザーに確認し、整理方針を決める
2. **Phase 1 実施**：ログファイル7本を `logs/app/` へ移動、`debug_columns_*.csv` を削除
3. **Phase 2 実施**（ユーザー承認後）：
   - `src/main.py` の `log_folder` パスを `logs/app/` に変更
   - `shimamura_search_handler.py` の summary・screenshot の出力先を変更
   - 新規フォルダを作成し `.gitkeep` を置く
4. **HANDOFF の Title列インデックス修正**（前セッションからの積み残し）：
   - `data/debug_columns_2026-03-26.csv` を確認し、`確認中_0326` が入っている列番号を特定
   - `_extract_results()` の `title = tds[1]` を正しいインデックスに修正
   - デバッグ用CSV出力コードを削除してクリーンアップ

## 注意点・ブロッカー

- `debug_columns_*.csv` は Title 列インデックス特定のための一時ファイル。削除は Title 列修正後に行うこと
- Phase 2 でパスを変更した場合、`config/main.yaml` の `app.download_dir` と整合性を確認すること（現状 `"data/downloads/"` で一致している）
- `test_handlers.py`（ルート直下）と `tests/` 配下のテストが重複している可能性がある。`python -m pytest tests/ -v` での実行対象に含まれていないか確認すること
- `batch_error_logs/` の `validation_error_*.log` が36本溜まっている。定期削除の仕組みがないため、フォルダ整理と同時に `.gitignore` 対象であることを再確認すること
